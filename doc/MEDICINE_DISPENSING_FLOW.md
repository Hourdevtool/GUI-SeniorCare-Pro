# 📋 หลักการทำงานระบบจ่ายยา (Medicine Dispensing System)

## 🎯 ภาพรวมระบบ

ระบบจ่ายยาทำงานผ่านการสื่อสารระหว่าง **Python (Raspberry Pi)** กับ **ESP32** ผ่าน **Serial Port** เพื่อควบคุมการจ่ายยาอัตโนมัติตามเวลาที่กำหนด

---

## 🔄 Flow การทำงานหลัก

### **Phase 1: การตั้งค่าเวลาจ่ายยา (Schedule Setup)**

```
┌─────────────────────────────────────────────────────────┐
│ 1. ผู้ใช้ตั้งค่าเวลาจ่ายยาใน MedicationApp                │
│    - เลือกมื้อ (เช้า, กลางวัน, เย็น, ก่อนนอน)            │
│    - กำหนดเวลา (HH:MM)                                    │
│    - เลือกยา (สูงสุด 4 ตัวต่อมื้อ)                        │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 2. บันทึกข้อมูลลงฐานข้อมูล                                 │
│    - Online: ส่งไปยัง server ทันที                        │
│    - Offline: บันทึกใน queue file (offline_schedule_queue.json) │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 3. อัพเดต schedule ใน serial_handler                     │
│    - เรียก update_schedule_times()                       │
│    - แปลงข้อมูลเป็นรูปแบบ "HH:MM"                        │
│    - เก็บใน allTime[]                                    │
└─────────────────────────────────────────────────────────┘
```

---

### **Phase 2: Main Loop (Serial Handler)**

ระบบทำงานใน **infinite loop** โดยทำ 4 งานหลักพร้อมกัน:

#### **2.1 ตรวจสอบจำนวนยาคงเหลือ (Medicine Count Check)**

```python
ทุก loop iteration:
  ├─ ดึงจำนวนยาจาก user['count_medicine']
  ├─ ถ้าจำนวนยา = 0 และยังไม่เคยส่ง reset_data:
  │   └─ ส่งคำสั่งรีเซ็ต → TX: {"cmd":0, "message":"reset_data"}
  ├─ ถ้าจำนวนยา = 28 (เต็มถาด):
  │   └─ ส่งคำสั่งรีเซ็ต → TX: {"cmd":0, "message":"reset_data"}
  └─ ถ้าจำนวนยา > 0 และ < 28:
      └─ Reset flag เพื่อให้ส่งได้อีกครั้งเมื่อยา = 0
```

**เหตุผล:**
- เมื่อยา = 0 หมายความว่าจ่ายยาครบ 28 รอบแล้ว ต้องรีเซ็ต ESP32
- เมื่อยา = 28 หมายความว่าเติมยาใหม่แล้ว ต้องรีเซ็ต ESP32

---

#### **2.2 ตรวจสอบคำสั่งจ่ายยาทันที (Instant Dispense)**

```python
ทุก loop iteration:
  ├─ ตรวจสอบ _instant_dispense_event
  ├─ ถ้า event ถูก set (จาก Test Mode หรือ UI):
  │   ├─ Clear event
  │   └─ ส่งคำสั่งจ่ายยาทันที → TX: {"cmd":1, "message":"init"}
  └─ ถ้า event ไม่ถูก set:
      └─ ข้าม
```

**ใช้เมื่อ:**
- กดปุ่ม "จ่ายยาทันที" ใน Test Mode
- ต้องการทดสอบการจ่ายยา

---

#### **2.3 รับข้อมูลจาก ESP32 (Receive Data)**

```python
ทุก loop iteration:
  ├─ รอรับข้อมูลจาก ESP32 (timeout 5 วินาที)
  ├─ ถ้าได้รับข้อมูล JSON (dict):
  │   ├─ อัพเดต battery level → battery_var
  │   ├─ อัพเดต status → status_var
  │   ├─ ตรวจสอบสถานะ:
  │   │   ├─ "complete" (status=1) → จ่ายยาสำเร็จ
  │   │   │   ├─ เล่นเสียง "จ่ายยาสำเร็จค่ะ"
  │   │   │   ├─ ลดจำนวนยาใน UI
  │   │   │   ├─ บันทึกประวัติการจ่ายยา (success)
  │   │   │   └─ ส่งแจ้งเตือน LINE
  │   │   ├─ "fail" (status=0) → จ่ายยาล้มเหลว
  │   │   │   ├─ เล่นเสียง "ดันยาไม่สำเร็จค่ะ"
  │   │   │   ├─ บันทึกประวัติการจ่ายยา (failed)
  │   │   │   └─ ส่งแจ้งเตือน LINE
  │   │   └─ "dontpick" (dontpickX) → ผู้ป่วยไม่รับยา
  │   │       ├─ เล่นเสียง "ผู้ป่วยกรุณารับยาด้วยค่ะ"
  │   │       ├─ นับจำนวนครั้ง (dontpick_count)
  │   │       └─ ถ้า dontpick_count >= threshold:
  │   │           └─ Trigger SOS Call อัตโนมัติ
  │   └─ ถ้าได้รับข้อความ (str):
  │       └─ อัพเดต status_var ด้วยข้อความพิเศษ
  └─ ถ้า timeout → วน loop ต่อไป
```

**สถานะที่สำคัญ:**
- `status=0` หรือ `"fail"` → จ่ายยาล้มเหลว
- `status=1` หรือ `"complete"` → จ่ายยาสำเร็จ
- `"dontpick1"`, `"dontpick2"`, ... → ผู้ป่วยไม่รับยา (จำนวนครั้ง)

---

#### **2.4 ตรวจสอบเวลาจ่ายยา (Schedule Check)**

```python
ทุก loop iteration:
  ├─ ดึง schedule_times จาก allTime[]
  ├─ ตรวจสอบเวลาปัจจุบันกับเวลาจ่ายยาที่ตั้งไว้
  ├─ สำหรับแต่ละ schedule:
  │   ├─ คำนวณความแตกต่างเวลา (diff)
  │   ├─ ถ้าถึงเวลาจ่ายยา (0 <= diff <= 60 วินาที):
  │   │   ├─ ตรวจสอบว่าเคยส่งคำสั่งนี้แล้วหรือยัง
  │   │   ├─ ถ้ายังไม่เคยส่ง:
  │   │   │   ├─ ส่งคำสั่งจ่ายยา → TX: {"cmd":1, "message":"init"}
  │   │   │   ├─ บันทึก schedule_key เข้า _triggered_schedule_keys
  │   │   │   └─ ส่งแจ้งเตือน LINE
  │   │   └─ ถ้าเคยส่งแล้ว → ข้าม (ป้องกันส่งซ้ำ)
  │   └─ ถ้ายังไม่ถึงเวลา → ข้าม
  └─ รอ 0.01 วินาที (ป้องกัน busy loop)
```

**Tolerance Window:**
- **Before:** 0 วินาที (ไม่ส่งก่อนเวลา)
- **After:** 60 วินาที (ส่งได้ภายใน 60 วินาทีหลังถึงเวลา)

**ป้องกันการส่งซ้ำ:**
- ใช้ `_triggered_schedule_keys` เพื่อเก็บ schedule_key ที่ส่งแล้ว
- Schedule key = `"YYYY-MM-DD HH:MM:SS"`

---

## 📊 Flow Diagram แบบละเอียด

```
┌─────────────────────────────────────────────────────────┐
│              STARTUP PHASE                               │
├─────────────────────────────────────────────────────────┤
│ 1. เปิด Serial Port (/dev/serial0, 115200 baud)         │
│ 2. รอ 2 วินาที                                          │
│ 3. ส่ง {"cmd":0, "message":"init"}                      │
│ 4. รอรับ status แรกจาก ESP32                           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              MAIN LOOP (Infinite)                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────┐      │
│  │ 1. ตรวจสอบจำนวนยาคงเหลือ                      │      │
│  │    IF count_medicine == 0:                   │      │
│  │      → ส่ง {"cmd":0, "message":"reset_data"} │      │
│  │    IF count_medicine == 28:                   │      │
│  │      → ส่ง {"cmd":0, "message":"reset_data"} │      │
│  └──────────────────────────────────────────────┘      │
│                        ↓                                 │
│  ┌──────────────────────────────────────────────┐      │
│  │ 2. ตรวจสอบ Instant Dispense                   │      │
│  │    IF _instant_dispense_event.is_set():      │      │
│  │      → ส่ง {"cmd":1, "message":"init"}       │      │
│  └──────────────────────────────────────────────┘      │
│                        ↓                                 │
│  ┌──────────────────────────────────────────────┐      │
│  │ 3. รับข้อมูลจาก ESP32                         │      │
│  │    RX: {"battery":85, "status":1}           │      │
│  │    ├─ อัพเดต battery_var                     │      │
│  │    ├─ อัพเดต status_var                      │      │
│  │    └─ ตรวจสอบสถานะและดำเนินการ:              │      │
│  │        ├─ complete → เล่นเสียง, ลดยา, บันทึก  │      │
│  │        ├─ fail → เล่นเสียง, บันทึก            │      │
│  │        └─ dontpick → เล่นเสียง, นับครั้ง, SOS │      │
│  └──────────────────────────────────────────────┘      │
│                        ↓                                 │
│  ┌──────────────────────────────────────────────┐      │
│  │ 4. ตรวจสอบเวลาจ่ายยา                          │      │
│  │    FOR each schedule in allTime[]:            │      │
│  │      IF ถึงเวลา (0 <= diff <= 60s):          │      │
│  │        AND ยังไม่เคยส่ง:                      │      │
│  │          → ส่ง {"cmd":1, "message":"init"}   │      │
│  │          → บันทึก schedule_key               │      │
│  └──────────────────────────────────────────────┘      │
│                        ↓                                 │
│  ┌──────────────────────────────────────────────┐      │
│  │ 5. รอ 0.01 วินาที                             │      │
│  └──────────────────────────────────────────────┘      │
│                        ↓                                 │
│                    วน Loop ต่อไป                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 สถานการณ์สำคัญ

### **สถานการณ์ที่ 1: ถึงเวลาจ่ายยา (14:30)**

```
เวลา: 14:30:00
├─ ตรวจสอบ: ถึงเวลาจ่ายยา (diff = 0, อยู่ใน tolerance 60s)
├─ ตรวจสอบ: schedule_key = "2024-01-15 14:30:00" ยังไม่เคยส่ง
├─ ส่งคำสั่ง: TX: {"cmd":1, "message":"init"}
├─ บันทึก: เพิ่ม "2024-01-15 14:30:00" เข้า _triggered_schedule_keys
└─ ส่งแจ้งเตือน: "จ่ายยาตามเวลาสำเร็จ"

เวลา: 14:30:01
└─ รับคำตอบ: RX: {"battery":85, "status":1}
   ├─ อัพเดต status_var = "1"
   ├─ ตรวจสอบ: normalized_status = "complete"
   ├─ เล่นเสียง: "จ่ายยาสำเร็จค่ะ"
   ├─ ลดจำนวนยา: count_medicine -= 1
   ├─ บันทึกประวัติ: success
   └─ ส่งแจ้งเตือน LINE: "การจ่ายยาสำเร็จ"

เวลา: 14:31:00 (ยังอยู่ใน tolerance)
├─ ตรวจสอบ: ถึงเวลาจ่ายยา (diff = 60, อยู่ใน tolerance 60s)
├─ ตรวจสอบ: schedule_key = "2024-01-15 14:30:00" เคยส่งแล้ว
└─ ข้าม: ไม่ส่งซ้ำ
```

---

### **สถานการณ์ที่ 2: ผู้ป่วยไม่รับยา (dontpick)**

```
เวลา: 14:30:00
├─ ส่งคำสั่ง: TX: {"cmd":1, "message":"init"}
└─ ESP32 เริ่มจ่ายยา

เวลา: 14:30:05
└─ รับสถานะ: RX: {"battery":85, "status":"dontpick1"}
   ├─ ตรวจสอบ: dontpick_count = 1
   ├─ เล่นเสียง: "ผู้ป่วยกรุณารับยาด้วยค่ะ"
   ├─ ตรวจสอบ: 1 < threshold (6) → ยังไม่ trigger SOS
   └─ อัพเดต last_dontpick_count = 1

เวลา: 14:30:10
└─ รับสถานะ: RX: {"battery":85, "status":"dontpick2"}
   ├─ ตรวจสอบ: dontpick_count = 2
   ├─ เล่นเสียง: "ผู้ป่วยกรุณารับยาด้วยค่ะ"
   ├─ ตรวจสอบ: 2 < threshold (6) → ยังไม่ trigger SOS
   └─ อัพเดต last_dontpick_count = 2

... (ทำซ้ำจนถึง dontpick6)

เวลา: 14:30:30
└─ รับสถานะ: RX: {"battery":85, "status":"dontpick6"}
   ├─ ตรวจสอบ: dontpick_count = 6
   ├─ เล่นเสียง: "ผู้ป่วยกรุณารับยาด้วยค่ะ"
   ├─ ตรวจสอบ: 6 >= threshold (6) → trigger SOS!
   ├─ ส่งแจ้งเตือน LINE: "ผู้ป่วยไม่รับยา 6/6 รอบ"
   └─ Trigger SOS Call อัตโนมัติ
```

---

### **สถานการณ์ที่ 3: จำนวนยาคงเหลือ = 0**

```
Loop iteration:
├─ ตรวจสอบ: count_medicine = 0
├─ ตรวจสอบ: ยังไม่เคยส่ง reset_data (zero_cycle_reset_sent = False)
├─ ส่งคำสั่ง: TX: {"cmd":0, "message":"reset_data"}
├─ บันทึก: zero_cycle_reset_sent = True
└─ ESP32 ตอบกลับ: RX: {"battery":85, "status":0}
```

---

### **สถานการณ์ที่ 4: Test Mode - จ่ายยาทันที**

```
ผู้ใช้กดปุ่ม "จ่ายยาทันที" ใน Test Mode:
├─ เรียก request_instant_dispense_command()
├─ Set _instant_dispense_event
└─ Main Loop ตรวจพบ event:
   ├─ Clear event
   └─ ส่งคำสั่ง: TX: {"cmd":1, "message":"init"}
      └─ ESP32 จ่ายยาทันที (ไม่รอ schedule)
```

---

## 🔑 Key Features

### **1. ป้องกันการส่งซ้ำ (Duplicate Prevention)**

- **Schedule:** ใช้ `_triggered_schedule_keys` เพื่อป้องกันส่งคำสั่งจ่ายยาซ้ำ
- **Reset Data:** ใช้ `zero_cycle_reset_sent` เพื่อป้องกันส่ง reset_data ซ้ำ
- **Status Update:** ใช้ `last_payload` เพื่อป้องกันอัพเดต status ซ้ำ

### **2. Tolerance Window**

- **Before:** 0 วินาที (ไม่ส่งก่อนเวลา)
- **After:** 60 วินาที (ส่งได้ภายใน 60 วินาทีหลังถึงเวลา)
- **เหตุผล:** รองรับความผิดพลาดของนาฬิกาและ network delay

### **3. Real-time Status Update**

- รับข้อมูลสถานะจาก ESP32 ทุกวินาที
- อัพเดต battery และ status แบบ real-time
- ตอบสนองทันทีเมื่อสถานะเปลี่ยน

### **4. Medicine Count Monitoring**

- ตรวจสอบจำนวนยาคงเหลือทุก loop iteration
- ส่ง reset_data อัตโนมัติเมื่อจำนวนยา = 0 หรือ = 28
- อัพเดต UI ทันทีเมื่อจำนวนยาเปลี่ยน

### **5. Offline Support**

- บันทึก schedule ใน queue file เมื่อ offline
- Sync ข้อมูลเมื่อกลับมา online
- Cache ข้อมูลใน local file

---

## 📝 คำสั่งที่ใช้

| คำสั่ง | เมื่อไหร่ | TX | RX (Expected) | หมายเหตุ |
|--------|----------|-----|--------------|----------|
| **Init** | เริ่มต้นระบบ | `{"cmd":0, "message":"init"}` | `{"battery":X, "status":0}` | ส่งครั้งเดียวตอนเริ่มต้น |
| **Dispense** | ถึงเวลาจ่ายยา / Instant | `{"cmd":1, "message":"init"}` | `{"battery":X, "status":1}` | ส่งเมื่อถึงเวลาหรือกดปุ่ม |
| **Reset** | จำนวนยา = 0 หรือ = 28 | `{"cmd":0, "message":"reset_data"}` | `{"battery":X, "status":0}` | ส่งอัตโนมัติเมื่อยา = 0/28 |

---

## 🎵 การเล่นเสียง

| สถานะ | เสียง | เมื่อไหร่ |
|-------|------|----------|
| **complete** | "จ่ายยาสำเร็จค่ะ" | เมื่อ status = 1 หรือ "complete" |
| **fail** | "ดันยาไม่สำเร็จค่ะ" | เมื่อ status = 0 หรือ "fail" |
| **dontpick** | "ผู้ป่วยกรุณารับยาด้วยค่ะ" | เมื่อ status = "dontpickX" (ทุกครั้งที่ count เปลี่ยน) |
| **startup** | "สวัสดีค่ะโฮมแคร์พร้อมให้บริการค่ะ" | เมื่อเริ่มระบบ (วันละครั้ง) |

---

## 📊 การบันทึกประวัติ

### **เมื่อจ่ายยาสำเร็จ (complete):**
1. ดึง medicine_id จาก schedule ที่ตรงกับเวลาปัจจุบัน
2. บันทึกลงฐานข้อมูล (status = "success")
3. ส่งแจ้งเตือน LINE

### **เมื่อจ่ายยาล้มเหลว (fail):**
1. ดึง medicine_id จาก schedule ที่ตรงกับเวลาปัจจุบัน
2. บันทึกลงฐานข้อมูล (status = "failed")
3. ส่งแจ้งเตือน LINE

---

## ⚠️ หมายเหตุสำคัญ

1. **Loop ทำงานตลอดเวลา** (infinite loop)
2. **ใช้ `time.sleep(0.01)`** เพื่อป้องกัน busy loop
3. **รับข้อมูลจาก ESP32 แบบ non-blocking** (timeout 5 วินาที)
4. **ตรวจสอบจำนวนยาคงเหลือทุก loop iteration**
5. **Schedule key ใช้รูปแบบ** `"YYYY-MM-DD HH:MM:SS"`
6. **รองรับ offline mode** โดยบันทึกใน queue file
7. **Test Mode** ใช้ email `"siri@gmail.com"` เพื่อแสดง UI

---

## 🔧 การแก้ไขปัญหา

### **ปัญหาที่ 1: ส่งคำสั่งซ้ำ**
- **สาเหตุ:** ไม่ได้ตรวจสอบ `_triggered_schedule_keys`
- **แก้ไข:** เพิ่มการตรวจสอบ schedule_key ก่อนส่งคำสั่ง

### **ปัญหาที่ 2: ไม่เล่นเสียง dontpick**
- **สาเหตุ:** ไม่มีการเรียก `sound_callback("dontpick")`
- **แก้ไข:** เพิ่มการเล่นเสียงเมื่อพบ dontpick status

### **ปัญหาที่ 3: ไม่ trigger SOS เมื่อเปลี่ยน threshold**
- **สาเหตุ:** `dontpick_sos_triggered` ยังเป็น True
- **แก้ไข:** รีเซ็ต flag เมื่อ threshold เปลี่ยน

---

## 📚 ไฟล์ที่เกี่ยวข้อง

- `lib/serial_handler.py` - Main loop และการสื่อสารกับ ESP32
- `main.py` - UI และการจัดการข้อมูล
- `lib/alert.py` - การส่งแจ้งเตือน LINE
- `lib/call.py` - การโทร SOS อัตโนมัติ
- `TIMELINE_SERIAL_HANDLER.md` - เอกสารอธิบาย timeline

---

**อัพเดตล่าสุด:** 2024-01-15

